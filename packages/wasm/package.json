{
  "name": "@kirily/wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "wasm-pack build ../../crates/kirily-wasm --target web --out-dir ../../packages/wasm/pkg --out-name kirily_wasm --release",
    "build:dev": "wasm-pack build ../../crates/kirily-wasm --target web --out-dir ../../packages/wasm/pkg --out-name kirily_wasm --dev",
    "test": "bun test"
  },
  "dependencies": {
    "@kirily/contract": "workspace:*",
    "@kirily/image-core": "workspace:*"
  }
}
