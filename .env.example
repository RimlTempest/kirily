# ブラウザへ公開してよい値だけ PUBLIC_ を付ける。
PUBLIC_APP_ENV=development
# local | remote。remote はユーザーの明示的な同意を前提にする（ADR-0004）。
PUBLIC_AI_MODE=local

# 以下はサーバー側のみ。Cloudflare の secret に置き、git へは書かない。
# AI_API_KEY=
# REMOTE_AI_URL=
